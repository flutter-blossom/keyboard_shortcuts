name: keyboard_shortcuts
description: A Widget to add Keyboard shortcuts
version: 0.1.4
author: Antoine MARCEL <amarcel.pro@gmail.com>
homepage: https://github.com/AntoineMarcel/keyboard_shortcuts

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  visibility_detector: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter